apiVersion: v1
kind: Service
metadata:
  name: {{ include "message-service-chart.fullname" . }}
  labels:
    {{- include "message-service-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "message-service-chart.selectorLabels" . | nindent 4 }}
